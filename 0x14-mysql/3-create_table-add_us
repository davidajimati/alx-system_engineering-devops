CREATE TABLE tyrell_corp;
USE tyrell_corp;
CREATE TABLE nexus6 (	
     id INT PRIMARY KEY,
     name VARCHAR(50)
    );
INSERT INTO nexus6 (id, name)
     VALUES(1, Leon);

# add select permission to the user
GRANT SELECT ON tyrell_corp.nexus6 TO holberton_user;
